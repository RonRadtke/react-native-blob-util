package com.ReactNativeBlobUtil.Utils

// Referenced by the library's AndroidManifest.xml under this exact name. Open, like
// the Java class it replaces, so an app that subclassed it still compiles.
open class FileProvider : androidx.core.content.FileProvider()
